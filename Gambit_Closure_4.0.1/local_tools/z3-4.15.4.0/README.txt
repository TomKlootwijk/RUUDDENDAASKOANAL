Z3 4.15.4.0, extracted from the existing offline pip wheel cache after user approval.

Add the bin subdirectory to PATH in the validating process only. No package manager or global installation was used.

The wheel RECORD was checked before extraction (38 hashed members). Extracted files and cache identity are recorded in PROVENANCE.json. Upstream metadata and RECORD are preserved in metadata/.

The cached wheel declares License: MIT in METADATA but contains no LICENSE file. The declaration is preserved verbatim; a full upstream license notice was not fetched or reconstructed.
